services:
  mcp-ocr:
    build: .
    container_name: mcp-ocr-server
    volumes:
      - ./images:/app/images
      - ./image:/app/image
      - ./output:/app/output
    stdin_open: true
    tty: true
    restart: unless-stopped
    working_dir: /app